---
phase: 03-task-details-data-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - include/kanban.h
  - src/models.c
  - src/storage.c
autonomous: true

requirements:
  - TSK-01
  - TSK-03
  - STO-02

must_haves:
  truths:
    - User can view task description in addition to title
    - User can edit task descriptions in Insert mode
    - File writes use atomic operations (temp file + fsync + rename)
  artifacts:
    - path: "include/kanban.h"
      provides: "Task struct with description field"
    - path: "src/models.c"
      provides: "Task model with description support"
    - path: "src/storage.c"
      provides: "Atomic file operations (already implemented)"
  key_links:
    - from: "Task struct"
      to: "storage.c write_markdown()"
      via: "description field serialized to markdown"
---

<objective>
Add task description support with atomic file operations. Extend Task model with description field, implement description popup overlay for viewing/editing, and verify atomic writes are working.
</objective>

<context>
@.planning/phases/03-task-details-data-safety/03-CONTEXT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

## Key Context from CONTEXT.md

- **D-01**: Descriptions displayed in popup overlay — pressing Enter or 'i' on a task opens a popup overlay showing the description
- **D-02**: Description can be edited in place within the popup
- **D-03**: Press Esc to close popup and return to board
- **D-04**: Descriptions stored as plain text only — no Markdown rendering
- **D-07**: STO-02 already implemented — storage.c uses temp file + fsync + rename pattern in write_markdown()
- **D-08**: No changes needed — atomic writes are working correctly per PITFALLS.md recommendations

## Task struct requirements

From `include/kanban.h`:
- Need to extend with description[MAX_DESC_LEN] field
- Checklist field (linked list or array) - will be added in plan 02

## Storage layer

From `src/storage.c:write_markdown()`:
- Already implements atomic writes (temp file + fsync + rename)
- Needs update to parse/write description to markdown format

</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Task model with description field</name>
  <files>include/kanban.h, src/models.c</files>
  <read_first>
    - include/kanban.h (current Task struct definition)
    - src/models.c (task_create, task_free functions)
  </read_first>
  <action>
    1. Add `description` field to Task struct in `include/kanban.h`:
       - Add: `#define MAX_DESC_LEN 500`
       - Add field: `char description[MAX_DESC_LEN];` to Task struct
       - Add field: `size_t desc_len;` to track length

    2. Update `task_create()` in `src/models.c`:
       - Initialize `description[0] = '\0'`
       - Initialize `desc_len = 0`

    3. Update `task_free()` in `src/models.c`:
       - No dynamic allocation in description, just zero out
  </action>
  <verify>
    <automated>grep -E "char description\[MAX|size_t desc_len" include/kanban.h && grep "description\[0\].*='\\\\0'" src/models.c</automated>
  </verify>
  <done>
    Task struct has description field, task_create initializes it, task_free handles cleanup
  </done>
</task>

<task type="auto">
  <name>Task 2: Update storage layer to serialize/parse descriptions</name>
  <files>src/storage.c</files>
  <read_first>
    - src/storage.c (write_markdown, parse_markdown functions)
    - Review current markdown format for task serialization
  </read_first>
  <action>
    1. Update `write_markdown()` to include description in output:
       - After task title, add "Description: <description>" line
       - Preserve plain text format (no markdown rendering per D-04)

    2. Update `parse_markdown()` to read description:
       - Parse "Description:" line from markdown
       - Populate task->description field

    3. Verify atomic write pattern still works:
       - write_markdown already uses temp + fsync + rename (STO-02)
       - Ensure description write is part of atomic operation
  </action>
  <verify>
    <automated>grep -A5 "Description:" src/storage.c | head -20</automated>
  </verify>
  <done>
    Descriptions persist to/from markdown file, atomic writes include description data
  </done>
</task>

<task type="auto">
  <name>Task 3: Create description popup overlay UI component</name>
  <files>src/ui.c, include/kanban.h</files>
  <read_first>
    - src/ui.c (existing render functions, window management)
    - src/input.c (key handling for modes)
    - Review Phase 2 modal editing for reuse pattern (src/modes.c if exists)
  </read_first>
  <action>
    1. Create `render_description_popup()` in `src/ui.c`:
       - Create centered overlay window (60% width, auto-height based on content)
       - Display task title as header
       - Display description in scrollable text area
       - Add border and title bar "Description"

    2. Add input handler for description popup mode:
       - Enter or 'i' key on selected task opens description popup
       - In popup: Arrow keys navigate within description text
       - Enter in popup enters edit mode for description
       - Esc closes popup and returns to board (per D-03)

    3. Add mode state: `MODE_DESCRIPTION_VIEW` and `MODE_DESCRIPTION_EDIT`
       - Extend AppMode enum in include/kanban.h

    4. Wire into main event loop:
       - Handle Enter/'i' key in Normal mode when task selected
       - Switch to DESCRIPTION_VIEW mode
       - Handle Esc to return to NORMAL mode
  </action>
  <verify>
    <automated>grep -E "render_description_popup|DESCRIPTION_VIEW|DESCRIPTION_EDIT" src/ui.c include/kanban.h | head -20</automated>
  </verify>
  <done>
    User can press Enter or 'i' on a task to view/edit description in popup overlay, Esc closes popup
  </done>
</task>

</tasks>

<verification>
- [ ] Task struct includes description[MAX_DESC_LEN] and desc_len fields
- [ ] task_create initializes description to empty string
- [ ] storage.c writes and parses Description: field to markdown
- [ ] Atomic writes (temp + fsync + rename) handle description data
- [ ] render_description_popup() creates overlay with title + description text
- [ ] Enter/'i' opens description popup from Normal mode
- [ ] Esc closes popup and returns to board
</verification>

<success_criteria>
- User can view task description in popup overlay
- User can edit description in-place within popup
- Description persists across app restarts via markdown storage
- File operations are atomic (no data corruption on crash)
</success_criteria>

<output>
After completion, create `.planning/phases/03-task-details-data-safety/{phase}-01-SUMMARY.md`
</output>