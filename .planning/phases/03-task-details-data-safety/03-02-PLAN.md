---
phase: 03-task-details-data-safety
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - include/kanban.h
  - src/ui.c
  - src/models.c
  - src/storage.c
  - src/input.c
autonomous: true

requirements:
  - TSK-02
  - TSK-04
  - TSK-05
  - TSK-06
  - TSK-07

must_haves:
  truths:
    - User can toggle between compact (title-only) and detailed views
    - Tasks support checklists with multiple subtasks
    - User can toggle checklist items between complete/incomplete
    - User can add new checklist items to any task
    - User can delete checklist items from tasks
  artifacts:
    - path: "include/kanban.h"
      provides: "ChecklistItem struct and Task.checklist field"
    - path: "src/ui.c"
      provides: "View toggle rendering, checklist display in detailed view"
    - path: "src/models.c"
      provides: "Checklist CRUD operations"
    - path: "src/storage.c"
      provides: "Checklist markdown serialization"
  key_links:
    - from: "Task struct"
      to: "checklist linked list"
      via: "task->checklist field"
    - from: "detailed view"
      to: "checklist rendering"
      via: "iterate over checklist items"
</context>

<objective>
Implement view toggle between compact and detailed views, and add checklist/subtask support to tasks.
</objective>

<context>
@.planning/phases/03-task-details-data-safety/03-CONTEXT.md
@.planning/phases/03-task-details-data-safety/03-01-PLAN.md

## Key Context from CONTEXT.md

- **D-05**: Agent's Discretion — specific key for toggling compact/detailed view, agent decides based on consistency with vim conventions
- **D-06**: Agent's Discretion — checklist markdown format, interaction model (sub-mode vs inline), and add/edit/delete workflow
- **Reusable Assets**: Task struct now has description field from plan 01

## Prior Plan Context

Plan 01 created:
- Task struct with description field
- Storage layer reads/writes Description: to markdown
- Description popup overlay for viewing/editing

## Implementation Notes

- View toggle key: Use 'v' key (like vim toggle) for consistency
- Checklist format in markdown: Use GitHub-style checkbox format:
  - [ ] unchecked item
  - [x] checked item
- Checklist interaction: Agent's discretion for sub-mode vs inline editing

</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ChecklistItem model to Task struct</name>
  <files>include/kanban.h, src/models.c</files>
  <read_first>
    - include/kanban.h (Task struct after plan 01)
    - src/models.c (existing model functions)
  </read_first>
  <action>
    1. Define ChecklistItem struct in include/kanban.h:
       ```c
       typedef struct ChecklistItem {
           char text[256];
           int checked;  // 0 = unchecked, 1 = checked
           struct ChecklistItem *next;
       } ChecklistItem;
       ```

    2. Add checklist field to Task struct:
       ```c
       ChecklistItem *checklist;
       ```

    3. Add checklist management functions in src/models.c:
       - `checklist_item_create(const char *text)` - allocate and init new item
       - `checklist_item_add(Task *task, const char *text)` - add to task's list
       - `checklist_item_toggle(ChecklistItem *item)` - toggle checked state
       - `checklist_item_delete(Task *task, ChecklistItem *item)` - remove from list
       - `checklist_free(Task *task)` - free all items

    4. Update task_free() to call checklist_free()
  </action>
  <verify>
    <automated>grep -E "typedef struct ChecklistItem|ChecklistItem \*checklist" include/kanban.h && grep "checklist_item_create" src/models.c</automated>
  </verify>
  <done>
    Task struct supports checklist linked list, CRUD operations available
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement view toggle (compact/detailed)</name>
  <files>src/ui.c, include/kanban.h</files>
  <read_first>
    - src/ui.c (render functions for tasks in columns)
    - include/kanban.h (current AppMode enum)
  </read_first>
  <action>
    1. Add view mode state to AppMode:
       - Add: `int detailed_view;` to AppState in include/kanban.h

    2. Create toggle function:
       - 'v' key in Normal mode toggles detailed_view flag
       - Store in AppState, persist via storage

    3. Update task rendering in src/ui.c:
       - In detailed_view mode: show task title + description + checklist
       - In compact mode: show only task title (current behavior)
       - Checklist display format in detailed view:
         ```
         ☐ Item 1
         ☑ Item 2
         ```

    4. Add visual indicator in status bar:
       - Show "[Compact]" or "[Detailed]" in status bar
  </action>
  <verify>
    <automated>grep -E "detailed_view|compact|Detailed" src/ui.c include/kanban.h | head -20</automated>
  </verify>
  <done>
    User can press 'v' to toggle between compact and detailed views, status bar shows current mode
  </done>
</task>

<task type="auto">
<parameter name>Task 3: Implement checklist interaction in detailed view</name>
  <files>src/input.c, src/ui.c</files>
  <read_first>
    - src/input.c (key handling in Normal and Insert modes)
    - src/ui.c (render functions, detailed view from task 2)
  </read_first>
  <action>
    1. Add checklist navigation in detailed view:
       - Arrow up/down when in detailed view moves through checklist items
       - After last checklist item, moves to description (if any)

    2. Add checklist toggle action:
       - Space key toggles checked state of current checklist item
       - Visual feedback: ☑/☐ indicator updates immediately

    3. Add checklist item creation:
       - Shift+N or ':addchecklist' opens mini-prompt to add new item
       - Input validation: max 255 chars, non-empty

    4. Add checklist item deletion:
       - dd or Delete key removes current checklist item
       - Confirmation not needed (quick delete for checklists)

    5. Update storage to serialize checklists:
       - Write checklist as GitHub-style markdown:
         ```
         - [ ] Item 1
         - [x] Item 2
         ```
       - Parse back on load
  </action>
  <verify>
    <automated>grep -E "checklist.*toggle|checklist.*add|checklist.*delete" src/input.c | head -20</automated>
  </verify>
  <done>
    User can navigate checklist items, toggle with Space, add with Shift+N, delete with dd/Delete
  </done>
</task>

</tasks>

<verification>
- [ ] ChecklistItem struct defined and integrated with Task
- [ ] 'v' key toggles between compact and detailed views
- [ ] Status bar shows current view mode
- [ ] Detailed view shows description + checklist items
- [ ] Space key toggles checklist item checked state
- [ ] Shift+N adds new checklist item to task
- [ ] dd/Delete removes checklist item
- [ ] Checklist persists to/from markdown file
</verification>

<success_criteria>
- View toggle works with 'v' key
- Detailed view shows description and checklist
- User can add, toggle, and delete checklist items
- Checklists persist to markdown file
</success_criteria>

<output>
After completion, create `.planning/phases/03-task-details-data-safety/{phase}-02-SUMMARY.md`
</output>